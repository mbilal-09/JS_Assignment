var pkr = (10 * 104.80) + (25 * 28);

document.getElementById("result").innerText = "Total currency in PKR: " + pkr;

// you can also do the above code by the method given below

// document.write("Total currency in PKR: " + pkr);